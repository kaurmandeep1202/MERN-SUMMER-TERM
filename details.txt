latest  26.3.0 n lts version -- long term support -- buy phone -- bugs recent bugs...

run the javascript file - node filename.js 

-17.57 left 


request respnse server 

varaibles -- var let const

ECMA SCRIPT SIX EC6

ARROW FUNCTION '

TEMPORAL DEADZONE 


HOISTING 


come out of current program - ctrl c 


dummyjson.com 


next.js - framework react -  admin panel -- ecommerce shop for selling an keychains  
automatic file based routing server rendering image font script 
react components, next.js frameworks , production web app 

react - ui library 
next.js - react + routing + rendering + backened  + optimization 

1. 
npm create vite@latest key-shop
select react
then javascript

npm run dev 

components 

react router dom - npm install react-router-dom


npm run dev -- --host

http://localhost:5173/
second one http://192.168.1.3:5173/


routing - 

rendering - 

data/api

demo 



to avoid flicker -- we will use native tags -- links tags
in header file 


virtual dom 

hooks 
1. use state - function component 
//what is use state? it is a hook that allows us to add state to our functional components. it returns an array with two elements: the current state and a function that updates the state. we can use it to create a counter that increments every time we click a button.
import { useState } from "react";
 
function Home() {
  const [counter, setCounter] = useState(0);
  const [btn, setBtnText] = useState('Hello');

  return (
   //simple welcome page for the key shop app
    <div className = "page">
      <section className="home">
        <h1>Welcome to Key Shop</h1>
        <h2> Current value of counter is : {counter}</h2>
        <p>Your one-stop shop for stylish and personalized key rings.</p>
        <button onClick={() => setCounter(counter + 1)}> Click Me</button>
        
        <br/>
        <br/>
       

        <button onClick={() => btn == 'Hello' ? setBtnText('Bye') : setBtnText('Hello')}>{btn} </button>




      </section>
    </div>
  );
}

export default Home;


//to install tailwind 
npm install tailwind @tailwindcss/vite 
in vite.config .. we have to import tailwind 
in css we have to write @import .. something 


destructuring
--object and arrays 


prop -- home({add to cart})
props in react youtube video 


48.00 19 june 20226


-- design home page 
 



mongodb atlas - online cloud - db 
compass - ui to view your db 
mongo db - database server



23 june --- 45.00

how to change the port number  
-- open vite.config.js 
-- write under plugin 
-- server: {
  port: 5000
}

-- now we will know about the cart of individual .. how many items are added to a particular cart block 
-- lets create a cart page 
-- now just go to app.jsx then comment out the const line
-- now we are just doing what type of items has been added 
-- const [cartItems, setCartItems] = useState([]);
-- 

25.04
we also added in app.jsx 
  const cartCount = cartItems.reduce(
    (total, item) => total + item.quantity,
    0
  );

then we have added cart page and then start making 

there is conditional rendering concept -- 
-- changes to app.jsx
changes to cart.jsx
changes to header.jsx
now we will do to css of app.css


25.june
-- 
u can also deploy in netlify, aws cloud, vercel 
so for starting u can do it on vercel 


u can deploy this app on vercel 
vercel.com
u havee to write on vs code terminal - 
vercel --prod 


we have just done increase and decrease on click of the button 
so first thing is to do 
-------- make a function of increaseQuantity in app.jsx 
 function increaseQuantity(productId){
    setCartItems((previousCartItems) => {
       return previousCartItems.map((item) => {
        return item.id === productId ? {...item, quantity:item.quantity + 1} : item;
       })
    })
  }

-----now at <route> section u just have to pass functions through props 
like this in app.jsx
<Route path="/cart" element={<Cart cartItems={cartItems}  increaseQuantity={increaseQuantity} decreaseQuantity={decreaseQuantity}/>}/>
</Routes>

-----then go to the cart.jsx -- call decreasequantity over there at starting 
function cart({cartItems, increaseQuantity, decreaseQuantity}) 

------- then where u have added button there u have to make changes with onclick 
<div className="quantityBox">
<button onClick = {() => decreaseQuantity(item.id)}> - </button>
<span>{item.quantity}</span>
<button onClick = {() => increaseQuantity(item.id)}> + </button>



----- at last quantity cannot be in minus so we have to use filter 
 function decreaseQuantity(productId){
    setCartItems((previousCartItems) => {
       return previousCartItems.map((item) => {
        return item.id === productId ? {...item, quantity:item.quantity - 1} : item;
       }).filter((item) => item.quantity > 0);
    })
  }


--search on google -- third party installation 
-- cart is added so there are toasts messages in react 
-- react-toastify and react-hot-toast 
install from npm install react-hot-toast from react hot toast website 
now just import on the top of app.jsx
add <Toaster under the header also and paste the code in add to cart 
 toast('Item added to the cart successfully!');
-- we can add few features from the website only to the toaster only. 
link https://react-hot-toast.com/



-- now we have added stock to the home.jsx
now we want to check if the stock is full and how many stock are there ? 



it will be very annoying when there is flooding of messages then 
add only toast.success with  {id:"cart-toast"};
function addToCart(product) {

  if (!product) {
    return;
  }

  setCartItems((previousCartItems) => {

    const productAlreadyInCart = previousCartItems.find(
      (item) => item.id === product.id
    );

    const quantity = productAlreadyInCart
      ? productAlreadyInCart.quantity
      : 0;

    if (product.stock < quantity + 1) {
      toast.error(`Sorry, ${product.name} is out of stock`, {
        id: "cart-toast",
      });

      return previousCartItems;
    }

    if (productAlreadyInCart) {

      toast.success(`${product.name} added to the cart successfully!`, {
        id: "cart-toast",
      });

      return previousCartItems.map((item) =>
        item.id === product.id
          ? {
              ...item,
              quantity: item.quantity + 1,
            }
          : item
      );
    }

    toast.success(`${product.name} added to the cart successfully!`, {
      id: "cart-toast",
    });

    return [...previousCartItems, { ...product, quantity: 1 }];

  }); // closes setCartItems
} // closes addToCart



=============================================================

NOWWWWWWWWWWWWWWWWWWWWWWWWWWWWW
CREATE ONE FOLDER WHICH IS key-shop-apis

1. Create the backend
--mkdir key-shop-apis
--cd  key-shop-apis
--npm init -y  // see the json file there -- iniitialte the project node -- package.json file will be created automatically after that command

--npm install express mongoose cors dotenv // third party library 
   for this whole command - // exprees - framwwork of node js -- just type first npm install express - u will get only node_modules folder
   now install -- npm install mongoose
   then npm install cors
   then npm install dotenv

--npm install -D nodemon  

Suggested Archtecture
key-shop-apis/
├── models/   //make folder
│   └── Product.js
├── routes/
│   └── productRoutes.js
├── .env
├── .gitignore
└── server.js

2. Configure environment variables
backend/.env:
PORT=5000
MONGODB_URI=mongodb://127.0.0.1:270

For MongoDB Atlas, use its connection string instead.

Never commit .env:
node_modules
.env

3. Create the product model
models/Product.js:
    
    import mongoose from "mongoose";

const productSchema = new mongoose.Schema(
  {
    name: {
      type: String,
      required: true,
      trim: true
    },
    description: {
      type: String,
      required: true
    },
    price: {
      type: Number,
      required: true,
      min: 0
    },
    image: {
      type: String,
      required: true
    },
    stock: {
      type: Number,
      required: true,
      min: 0
    }
  },
  {
    timestamps: true,
    toJSON: {
      virtuals: true,
      versionKey: false,
      transform(doc, object) {
        delete object._id;
      }
    }
  }
);

export default mongoose.model("Product", productSchema);

The JSON configuration exposes MongoDB’s _id as id, matching your existing product.id cart logic.

4. Create the products route
routes/productRoutes.js:
    
    import express from "express";
import Product from "../models/Product.js";

const router = express.Router();

router.get("/", async (req, res) => {
  try {
    const products = await Product.find();

    res.status(200).json({
      success: true,
      products
    });
  } catch (error) {
    res.status(500).json({
      success: false,
      message: "Unable to fetch products"
    });
  }
});

export default router;
GET http://localhost:5000/api/products
5. Create the Express server
server.js:
    
    import express from "express";
import mongoose from "mongoose";
import cors from "cors";
import dotenv from "dotenv";
import productRoutes from "./routes/productRoutes.js";

dotenv.config();

const app = express();

app.use(cors({ origin: "http://localhost:5173" }));
app.use(express.json());

app.use("/api/products", productRoutes);

mongoose
  .connect(process.env.MONGODB_URI)
  .then(() => {
    app.listen(process.env.PORT, () => {
      console.log(`Server running on port ${process.env.PORT}`);
    });
  })
  .catch((error) => {
    console.error("Database connection failed:", error.message);
  });

Add to backend/package.json:
    "type": "module",
"scripts": {
  "start": "node server.js",
  "dev": "nodemon server.js"
}
6. Add products to MongoDB
Create a products collection and insert documents such as:
    {
  "name": "Cartoon Key Chain",
  "description": "Colorful and cute design.",
  "price": 99,
  "image": "/images/shopping.webp",
  "stock": 10
}
7. Fetch products in Home.jsx
import { useEffect, useState } from "react";

const [products, setProducts] = useState([]);
const [loading, setLoading] = useState(true);
const [error, setError] = useState("");

useEffect(() => {
  async function fetchProducts() {
    try {
      const response = await fetch("http://localhost:5000/api/products");

      if (!response.ok) {
        throw new Error("Unable to fetch products");
      }

      const data = await response.json();
      setProducts(data.products);
    } catch (error) {
      setError(error.message);
    } finally {
      setLoading(false);
    }
  }

  fetchProducts();
}, []);
Handle loading and errors before rendering:

if (loading) return <p>Loading products...</p>;
if (error) return <p>{error}</p>;
Your existing products.map(...) can remain largely unchanged.1. Create the backend
mkdir key-shop-apis
cd  key-shop-apis
npm init -y
npm install express mongoose cors dotenv
npm install -D nodemon

Suggested Archtecture
key-shop-apis/
├── models/
│   └── Product.js
├── routes/
│   └── productRoutes.js
├── .env
├── .gitignore
└── server.js

2. Configure environment variables
backend/.env:
PORT=5000
MONGODB_URI=mongodb://127.0.0.1:270

For MongoDB Atlas, use its connection string instead.

Never commit .env:
node_modules
.env

3. Create the product model
models/Product.js:
    
    import mongoose from "mongoose";

const productSchema = new mongoose.Schema(
  {
    name: {
      type: String,
      required: true,
      trim: true
    },
    description: {
      type: String,
      required: true
    },
    price: {
      type: Number,
      required: true,
      min: 0
    },
    image: {
      type: String,
      required: true
    },
    stock: {
      type: Number,
      required: true,
      min: 0
    }
  },
  {
    timestamps: true,
    toJSON: {
      virtuals: true,
      versionKey: false,
      transform(doc, object) {
        delete object._id;
      }
    }
  }
);

export default mongoose.model("Product", productSchema);

The JSON configuration exposes MongoDB’s _id as id, matching your existing product.id cart logic.

4. Create the products route
routes/productRoutes.js:
    
    import express from "express";
import Product from "../models/Product.js";

const router = express.Router();

router.get("/", async (req, res) => {
  try {
    const products = await Product.find();

    res.status(200).json({
      success: true,
      products
    });
  } catch (error) {
    res.status(500).json({
      success: false,
      message: "Unable to fetch products"
    });
  }
});

export default router;
GET http://localhost:5000/api/products
5. Create the Express server
server.js:
    
    import express from "express";
import mongoose from "mongoose";
import cors from "cors";
import dotenv from "dotenv";
import productRoutes from "./routes/productRoutes.js";

dotenv.config();

const app = express();

app.use(cors({ origin: "http://localhost:5173" }));
app.use(express.json());

app.use("/api/products", productRoutes);

mongoose
  .connect(process.env.MONGODB_URI)
  .then(() => {
    app.listen(process.env.PORT, () => {
      console.log(`Server running on port ${process.env.PORT}`);
    });
  })
  .catch((error) => {
    console.error("Database connection failed:", error.message);
  });

Add to backend/package.json:
    "type": "module",
"scripts": {
  "start": "node server.js",
  "dev": "nodemon server.js"
}
6. Add products to MongoDB
Create a products collection and insert documents such as:
    {
  "name": "Cartoon Key Chain",
  "description": "Colorful and cute design.",
  "price": 99,
  "image": "/images/shopping.webp",
  "stock": 10
}
7. Fetch products in Home.jsx
import { useEffect, useState } from "react";

const [products, setProducts] = useState([]);
const [loading, setLoading] = useState(true);
const [error, setError] = useState("");

useEffect(() => {
  async function fetchProducts() {
    try {
      const response = await fetch("http://localhost:5000/api/products");

      if (!response.ok) {
        throw new Error("Unable to fetch products");
      }

      const data = await response.json();
      setProducts(data.products);
    } catch (error) {
      setError(error.message);
    } finally {
      setLoading(false);
    }
  }

  fetchProducts();
}, []);
Handle loading and errors before rendering:

if (loading) return <p>Loading products...</p>;
if (error) return <p>{error}</p>;
Your existing products.map(...) can remain largely unchanged.




 

